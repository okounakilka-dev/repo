# Assets

Keep only small previews here (<500KB each) for README/docs.
Large renders, `.blend`, `.dxf`, `generated_images/` stay local (see root `.gitignore`).

Suggested:
- `vpet-settings.png` — settings window screenshot
- `rpg-demo.png` — console gameplay
- `house-plan-preview.png` — small 800px preview
- `avatar-preview.png` — small Blender render
